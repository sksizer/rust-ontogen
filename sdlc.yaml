# Per-project SDLC plugin configuration.
# Shape reference: ~/.claude/plugins/sdlc/conventions/sdlc-yaml.md
# Populate `quality_checks:` via /sdlc:find-quality-checks, or by hand.

quality_checks:
  - just full-check

lease_authority: origin
