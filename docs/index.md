# SDLC

> The single front-door for SDLC. The **Narrative** lives in the prose
> docs — [[vision]] (the why), [[PR-0001-sdlc]] (what it is and how it's
> designed), [[D-VSLI-distributed-work-runner-architecture]] (the architecture). The
> **Reference** below is generated from the entities on disk by the
> `docs` skill. **Do not hand-edit** — edit the source entity and
> regenerate.

---

# Reference

> Generated by the `docs` skill from entities on disk. Summary cells
> transclude each entity's `^summary` block, so they never drift. Each
> section splits into an **Active** table (in effect today) and, when
> non-empty, a follow-up **Emerging / obsoleted** table.

## Start here

| Doc                           | What it is                                     |
| ----------------------------- | ---------------------------------------------- |
| [[vision]]                    | The why — short read                           |
| [[PR-0001-sdlc]]               | The product — what it is and how it's designed |
| [[D-VSLI-distributed-work-runner-architecture]] | The system architecture                        |
| [[glossary]]                  | Vocabulary — every architecture term, defined once |
| [[references]]                | External documentation, research, and artifacts SDLC leans on |

## Products

The project may be **multi-product**, starting with one. Source:
[`./planning/products/`](./planning/products/).

| Product | Status | What it is |
| --- | --- | --- |

## Drivers

What SDLC is trying to solve, by `kind`. Source:
[`./planning/drivers/`](./planning/drivers/).

## Milestones

Milestones are product releases; order is ascending `version`. Source:
[`./planning/milestones/`](./planning/milestones/).

## Features

Capabilities with `audience: user` — abilities whose direct consumer
is an end user. Source:
[`./planning/capabilities/`](./planning/capabilities/).

*None drafted yet — the Executable Task lifecycle is
`audience: system` (see Technical capabilities). Product-facing
Features get drafted as their releases schedule them.*

## Data model

| Source                                             | What it is                                                            |
| -------------------------------------------------- | --------------------------------------------------------------------- |
| [[D-ORMG-data-model]]                               | Authoritative roster — every entity by layer, with status and purpose |
| [entity-model](./planning/decisions/entity-model/) | Design rationale and cross-entity relationships                       |
| [[D-7F2M-why-what-verify-chain]]                    | The why→what→verify chain threaded through the Capability hinge       |

## Principles

Source: [`./planning/principles/`](./planning/principles/).

## Standards

Source: [`./planning/standards/`](./planning/standards/).

## Architecture decisions (ADRs)

Source: [`./planning/decisions/`](./planning/decisions/). Folder ADRs
link inline; single-file ADRs transclude their `^summary`.

## Technical capabilities

Capabilities with `audience: system` — consumed by the system itself.
Source:
[`./planning/capabilities/`](./planning/capabilities/). The capability
map (in [[PR-0001-sdlc]]) extends the tree across all areas; today the
worked-out instance is **Executable Task** — a root plus nine lifecycle
sub-features.

---

# Appendix

## Glossary

SDLC architecture vocabulary, defined once. Standalone artifact:
[[glossary]]; source: [`./planning/terms/`](./planning/terms/).

## References

External documentation, research, and artifacts SDLC leans on.
Standalone artifact: [[references]]; source:
[`./planning/references/`](./planning/references/).

*None catalogued yet — `sdlc reference create` mints the first.*

## Conventions

| Convention | Meaning |
| ---------- | ------- |
| **Wikilinks** `[[name]]` | Resolve by filename match. Catalogued entities use `<prefix><N>-<slug>`; milestones use `[[<id>]]` (random base-36, e.g. `[[M-ML68]]`; legacy `M0000`/`M0001` grandfathered) |
| **Transclusion** `![[file#^summary]]` | Embeds an entity's summary block — how the summary cells stay in sync with their source entities |
| **Markdown links** `[name](path)` | Used for ADR folders and directories where wikilinks would be awkward |
