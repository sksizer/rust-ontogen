//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::Note;
use crate::schema::{CreateNoteInput, UpdateNoteInput};
use crate::store::Store;
use crate::store::note::NoteUpdate;

/// List all notes
pub async fn list(store: &Store) -> Result<Vec<Note>, AppError> {
    store.list_notes(None, None).await
}

/// Get a single note by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<Note, AppError> {
    store.get_note(id).await
}

/// Create a new note
pub async fn create(store: &Store, input: CreateNoteInput) -> Result<Note, AppError> {
    let note: Note = input.into();
    store.create_note(note).await
}

/// Update an existing note
pub async fn update(store: &Store, id: &str, input: UpdateNoteInput) -> Result<Note, AppError> {
    let updates: NoteUpdate = input.into();
    store.update_note(id, updates).await
}

/// Delete a note by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_note(id).await
}
