pub struct AllPrims {
    pub a: bool,
    pub b: u8,
    pub c: u16,
    pub d: u32,
    pub e: u64,
    pub f: i8,
    pub g: i16,
    pub h: i32,
    pub i: i64,
    pub j: f32,
    pub k: f64,
    pub l: String,
}
