//! Generated by ontogen. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::schema::Exercise;

/// Frontmatter view of a Exercise: every persisted field except the id (the
/// filename stem), the markdown body, and derived `has_many` views.
/// Relation ids are wikilink-encoded here and only here.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ExerciseFrontmatter {
    pub name: String,
    pub muscle_group: String,
    pub equipment: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notes: Option<String>,
}

/// The frontmatter keys this type owns — the full field set, so
/// `merge_serialize` removes cleared options and preserves hand-added keys.
pub const EXERCISE_FM_FIELDS: &[&str] = &["name", "muscle_group", "equipment", "notes"];

impl ExerciseFrontmatter {
    pub fn from_exercise(value: &Exercise) -> Self {
        Self {
            name: value.name.clone(),
            muscle_group: value.muscle_group.clone(),
            equipment: value.equipment.clone(),
            notes: value.notes.clone(),
        }
    }

    pub fn into_exercise(self, id: String) -> Exercise {
        Exercise { id, name: self.name, muscle_group: self.muscle_group, equipment: self.equipment, notes: self.notes }
    }
}
