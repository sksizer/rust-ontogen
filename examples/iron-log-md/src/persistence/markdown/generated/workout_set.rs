//! Generated by ontogen. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::schema::WorkoutSet;

/// Frontmatter view of a WorkoutSet: every persisted field except the id (the
/// filename stem), the markdown body, and derived `has_many` views.
/// Relation ids are wikilink-encoded here and only here.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkoutSetFrontmatter {
    pub workout_id: String,
    pub exercise_id: String,
    pub set_number: i32,
    pub weight_grams: i32,
    pub reps: i32,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub rpe: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notes: Option<String>,
}

/// The frontmatter keys this type owns — the full field set, so
/// `merge_serialize` removes cleared options and preserves hand-added keys.
pub const WORKOUT_SET_FM_FIELDS: &[&str] =
    &["workout_id", "exercise_id", "set_number", "weight_grams", "reps", "rpe", "notes"];

impl WorkoutSetFrontmatter {
    pub fn from_workout_set(value: &WorkoutSet) -> Self {
        Self {
            workout_id: markdown_store::wikilink::encode(&value.workout_id),
            exercise_id: markdown_store::wikilink::encode(&value.exercise_id),
            set_number: value.set_number,
            weight_grams: value.weight_grams,
            reps: value.reps,
            rpe: value.rpe,
            notes: value.notes.clone(),
        }
    }

    pub fn into_workout_set(self, id: String) -> WorkoutSet {
        WorkoutSet {
            id,
            workout_id: markdown_store::wikilink::strip(&self.workout_id),
            exercise_id: markdown_store::wikilink::strip(&self.exercise_id),
            set_number: self.set_number,
            weight_grams: self.weight_grams,
            reps: self.reps,
            rpe: self.rpe,
            notes: self.notes,
        }
    }
}
