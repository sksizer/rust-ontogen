//! Generated by ontogen. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::schema::Workout;

/// Frontmatter view of a Workout: every persisted field except the id (the
/// filename stem), the markdown body, and derived `has_many` views.
/// Relation ids are wikilink-encoded here and only here.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkoutFrontmatter {
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub name: Option<String>,
    pub date: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub duration_minutes: Option<i32>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub notes: Option<String>,
    #[serde(default, skip_serializing_if = "Vec::is_empty")]
    pub tags: Vec<String>,
    pub created_at: String,
}

/// The frontmatter keys this type owns — the full field set, so
/// `merge_serialize` removes cleared options and preserves hand-added keys.
pub const WORKOUT_FM_FIELDS: &[&str] = &["name", "date", "duration_minutes", "notes", "tags", "created_at"];

impl WorkoutFrontmatter {
    pub fn from_workout(value: &Workout) -> Self {
        Self {
            name: value.name.clone(),
            date: value.date.clone(),
            duration_minutes: value.duration_minutes,
            notes: value.notes.clone(),
            tags: value.tags.iter().map(|v| markdown_store::wikilink::encode(v)).collect(),
            created_at: value.created_at.clone(),
        }
    }

    pub fn into_workout(self, id: String) -> Workout {
        Workout {
            id,
            name: self.name,
            date: self.date,
            duration_minutes: self.duration_minutes,
            notes: self.notes,
            tags: markdown_store::wikilink::strip_vec(self.tags),
            created_at: self.created_at,
        }
    }
}
