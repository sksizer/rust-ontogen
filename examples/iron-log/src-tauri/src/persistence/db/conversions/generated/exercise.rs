//! Generated by ontogen. DO NOT EDIT.

use crate::persistence::db::entities::exercise as entity;
use crate::schema::Exercise;

impl Exercise {
    pub fn from_model(model: &entity::Model) -> Self {
        Self {
            id: model.id.clone(),
            name: model.name.clone(),
            muscle_group: model.muscle_group.clone(),
            equipment: model.equipment.clone(),
            notes: model.notes.clone(),
        }
    }

    pub fn to_active_model(&self) -> entity::ActiveModel {
        use sea_orm::Set;

        entity::ActiveModel {
            id: Set(self.id.clone()),
            name: Set(self.name.clone()),
            muscle_group: Set(self.muscle_group.clone()),
            equipment: Set(self.equipment.clone()),
            notes: Set(self.notes.clone()),
        }
    }
}
