//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::Workout;
use crate::schema::{CreateWorkoutInput, UpdateWorkoutInput};
use crate::store::Store;
use crate::store::workout::WorkoutUpdate;

/// List all workouts
pub async fn list(store: &Store) -> Result<Vec<Workout>, AppError> {
    store.list_workouts(None, None).await
}

/// Get a single workout by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<Workout, AppError> {
    store.get_workout(id).await
}

/// Create a new workout
pub async fn create(store: &Store, input: CreateWorkoutInput) -> Result<Workout, AppError> {
    let workout: Workout = input.into();
    store.create_workout(workout).await
}

/// Update an existing workout
pub async fn update(store: &Store, id: &str, input: UpdateWorkoutInput) -> Result<Workout, AppError> {
    let updates: WorkoutUpdate = input.into();
    store.update_workout(id, updates).await
}

/// Delete a workout by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_workout(id).await
}
