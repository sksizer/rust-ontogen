//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::Exercise;
use crate::schema::{CreateExerciseInput, UpdateExerciseInput};
use crate::store::Store;
use crate::store::exercise::ExerciseUpdate;

/// List all exercises
pub async fn list(store: &Store) -> Result<Vec<Exercise>, AppError> {
    store.list_exercises().await
}

/// Get a single exercise by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<Exercise, AppError> {
    store.get_exercise(id).await
}

/// Create a new exercise
pub async fn create(store: &Store, input: CreateExerciseInput) -> Result<Exercise, AppError> {
    let exercise: Exercise = input.into();
    store.create_exercise(exercise).await
}

/// Update an existing exercise
pub async fn update(store: &Store, id: &str, input: UpdateExerciseInput) -> Result<Exercise, AppError> {
    let updates: ExerciseUpdate = input.into();
    store.update_exercise(id, updates).await
}

/// Delete a exercise by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_exercise(id).await
}
