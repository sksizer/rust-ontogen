//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::Epic;
use crate::schema::{CreateEpicInput, UpdateEpicInput};
use crate::store::Store;
use crate::store::epic::EpicUpdate;

/// List all epics
pub async fn list(store: &Store) -> Result<Vec<Epic>, AppError> {
    store.list_epics(None, None).await
}

/// Get a single epic by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<Epic, AppError> {
    store.get_epic(id).await
}

/// Create a new epic
pub async fn create(store: &Store, input: CreateEpicInput) -> Result<Epic, AppError> {
    let epic: Epic = input.into();
    store.create_epic(epic).await
}

/// Update an existing epic
pub async fn update(store: &Store, id: &str, input: UpdateEpicInput) -> Result<Epic, AppError> {
    let updates: EpicUpdate = input.into();
    store.update_epic(id, updates).await
}

/// Delete a epic by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_epic(id).await
}
