//! Generated by ontogen. DO NOT EDIT.

use serde::{Deserialize, Serialize};

use crate::schema::Epic;

/// Frontmatter view of a Epic: every persisted field except the id (the
/// filename stem), the markdown body, and derived `has_many` views.
/// Relation ids are wikilink-encoded here and only here.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EpicFrontmatter {
    pub title: String,
    pub status: String,
}

/// The frontmatter keys this type owns — the full field set, so
/// `merge_serialize` removes cleared options and preserves hand-added keys.
pub const EPIC_FM_FIELDS: &[&str] = &["title", "status"];

impl EpicFrontmatter {
    pub fn from_epic(value: &Epic) -> Self {
        Self { title: value.title.clone(), status: value.status.clone() }
    }

    pub fn into_epic(self, id: String, body: String) -> Epic {
        Epic { id, body, title: self.title, status: self.status }
    }
}
