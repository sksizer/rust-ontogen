// Auto-generated transport layer. DO NOT EDIT.
// Generated by ontogen from API source files.

import type {
  CreateNoteInput,
  Note,
  UpdateNoteInput,
} from './types';

import { invoke } from '@tauri-apps/api/core';

// ── Transport Interface ──

export interface Transport {
  noteList(): Promise<Note[]>;
  noteGetById(id: string): Promise<Note>;
  noteCreate(input: CreateNoteInput): Promise<Note>;
  noteUpdate(id: string, input: UpdateNoteInput): Promise<Note>;
  noteDelete(id: string): Promise<null>;
}

// ── HTTP Helpers ──

const BASE = '/api';

async function httpGet<T>(path: string): Promise<T> {
  const res = await fetch(`${BASE}${path}`);
  if (!res.ok) {
    const body = await res.json().catch(() => ({ error: res.statusText }));
    throw new Error(body.error ?? res.statusText);
  }
  return res.json();
}

async function httpPost<T>(path: string, body?: unknown): Promise<T> {
  const res = await fetch(`${BASE}${path}`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: body != null ? JSON.stringify(body) : undefined,
  });
  if (!res.ok) {
    const errBody = await res.json().catch(() => ({ error: res.statusText }));
    throw new Error(errBody.error ?? res.statusText);
  }
  if (res.status === 204) return null as T;
  return res.json();
}

async function httpPut<T>(path: string, body: unknown): Promise<T> {
  const res = await fetch(`${BASE}${path}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  });
  if (!res.ok) {
    const errBody = await res.json().catch(() => ({ error: res.statusText }));
    throw new Error(errBody.error ?? res.statusText);
  }
  return res.json();
}

async function httpDelete(path: string): Promise<void> {
  const res = await fetch(`${BASE}${path}`, { method: 'DELETE' });
  if (!res.ok) {
    const errBody = await res.json().catch(() => ({ error: res.statusText }));
    throw new Error(errBody.error ?? res.statusText);
  }
}

function toQueryString(params: Record<string, unknown>): string {
  const parts: string[] = [];
  for (const [key, value] of Object.entries(params)) {
    if (value == null) continue;
    if (Array.isArray(value)) {
      for (const v of value) {
        parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(String(v))}`);
      }
    } else {
      parts.push(`${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`);
    }
  }
  return parts.length > 0 ? `?${parts.join('&')}` : '';
}

// ── HTTP Transport ──

export function createHttpTransport(): Transport {
  return {
    async noteList(): Promise<Note[]> {
      return httpGet('/notes');
    },
    async noteGetById(id: string): Promise<Note> {
      return httpGet(`/notes/${encodeURIComponent(id)}`);
    },
    async noteCreate(input: CreateNoteInput): Promise<Note> {
      return httpPost<Note>('/notes', input);
    },
    async noteUpdate(id: string, input: UpdateNoteInput): Promise<Note> {
      return httpPut<Note>(`/notes/${encodeURIComponent(id)}`, input);
    },
    async noteDelete(id: string): Promise<null> {
      await httpDelete(`/notes/${encodeURIComponent(id)}`);
      return null;
    },
  };
}

// ── IPC Transport ──

export function createIpcTransport(): Transport {
  return {
    async noteList(): Promise<Note[]> {
      return invoke('note_list');
    },
    async noteGetById(id: string): Promise<Note> {
      return invoke('note_get_by_id', { id });
    },
    async noteCreate(input: CreateNoteInput): Promise<Note> {
      return invoke('note_create', { input });
    },
    async noteUpdate(id: string, input: UpdateNoteInput): Promise<Note> {
      return invoke('note_update', { id, input });
    },
    async noteDelete(id: string): Promise<null> {
      await invoke('note_delete', { id });
      return null;
    },
  };
}
